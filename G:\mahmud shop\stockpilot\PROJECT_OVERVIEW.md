
# StockPilot অ্যাপ্লিকেশনটির সম্পূর্ণ ব্যাখ্যা (বাংলায়)

এই ডকুমেন্টটি আপনাকে StockPilot অ্যাপ্লিকেশনটির গঠন এবং কোড বুঝতে সাহায্য করবে।

## টেকনোলজি স্ট্যাক (Technology Stack)

অ্যাপ্লিকেশনটি আধুনিক কিছু টেকনোলজি ব্যবহার করে তৈরি করা হয়েছে:

- **Next.js**: এটি একটি React ফ্রেমওয়ার্ক যা সার্ভার-সাইড রেন্ডারিং (SSR) এবং দ্রুত পারফরম্যান্স নিশ্চিত করে।
- **React**: ইউজার ইন্টারফেস (UI) তৈরির জন্য ব্যবহৃত একটি জাভাস্ক্রিপ্ট লাইব্রেরি।
- **TypeScript**: কোডকে আরও শক্তিশালী এবং ত্রুটিমুক্ত করার জন্য জাভাস্ক্রিপ্টের একটি সুপারসেট।
- **Tailwind CSS**: দ্রুত এবং রেসপন্সিভ ডিজাইন করার জন্য একটি CSS ফ্রেমওয়ার্ক।
- **ShadCN UI**: আগে থেকে ডিজাইন করা এবং কাস্টমাইজযোগ্য UI কম্পונেন্ট (যেমন বাটন, কার্ড, ডায়ালগ)।
- **Firebase**: ব্যবহারকারী অথেনটিকেশন (লগইন, সাইনআপ) এর জন্য ব্যবহৃত হয়।
- **PostgreSQL**: ডেটাবেস হিসেবে ব্যবহৃত হয়, যা Docker দ্বারা পরিচালিত।
- **Docker & Docker Compose**: অ্যাপ্লিকেশন এবং ডেটাবেসকে কন্টেইনারে প্যাক করে সহজে চালানোর জন্য।

---

## প্রজেক্টের গঠন (Project Structure)

প্রজেক্টের মূল ফাইল এবং ফোল্ডারগুলো নিচে ব্যাখ্যা করা হলো:

### 📁 `src/app/`
এটি অ্যাপ্লিকেশনের প্রধান ফোল্ডার, যেখানে সব পেজ এবং রাউটিং পরিচালনা করা হয়।

- **`layout.tsx`**: এটি হলো পুরো অ্যাপ্লিকেশনের মূল লেআউট। এখানে `<html>` এবং `<body>` ট্যাগ থাকে এবং `Inter` ফন্ট লোড করা হয়েছে। `Toaster` কম্পונেন্টটিও এখানে যুক্ত করা হয়েছে, যা নোটিফিকেশন দেখানোর কাজ করে।

- **`login/page.tsx` এবং `signup/page.tsx`**: এই দুটি ফাইল যথাক্রমে লগইন এবং সাইনআপ পেজ তৈরি করে। এখানে Firebase Authentication ব্যবহার করে ব্যবহারকারীর ইমেল ও পাসওয়ার্ড যাচাই করা হয়।

- **`dashboard/`**: লগইন করার পর ব্যবহারকারী যা কিছু দেখতে পায়, তার সবকিছু এই ফোল্ডারের ভেতরে থাকে।
    - **`layout.tsx`**: এটি ড্যাশবোর্ডের লেআউট। উপরের হেডার এবং টপ নেভিগেশন বারটি এখানে যুক্ত করা হয়েছে।
    - **`page.tsx`**: ড্যাশবোর্ডের প্রধান পেজ, যেখানে আজকের এবং মাসিক বিক্রয়, লাভ, খরচ ইত্যাদি কার্ড আকারে দেখানো হয়।
    - **`products/page.tsx`**: "Products" পেজটি দেখায়। এখানে পণ্য যোগ করা, এডিট করা, ডিলেট করা এবং তালিকা দেখার ব্যবস্থা আছে।
    - **`invoice/page.tsx`**: নতুন চালান (Invoice) তৈরির পেজ। এখান থেকে পণ্য নির্বাচন করে গ্রাহকের জন্য বিল তৈরি করা যায়।
    - **`buyers/page.tsx`**: গ্রাহকদের তালিকা এবং তাদের কেনাকাটার ইতিহাস দেখার পেজ।
    - **`employees/page.tsx`**: কর্মচারীদের উপস্থিতি (Attendance) পরিচালনা করার পেজ।
    - **`salaries/page.tsx`**: কর্মচারীদের বেতন দেওয়ার পেজ।

---

### 📁 `src/components/`
এখানে পুনরায় ব্যবহারযোগ্য (reusable) UI কম্পונেন্টগুলো রাখা হয়।

- **`ui/`**: এই ফোল্ডারে `ShadCN` থেকে আসা বেসিক কম্পונেন্টগুলো (যেমন `button.tsx`, `card.tsx`, `input.tsx`) থাকে।
- **`site-header.tsx` ও `top-nav-bar.tsx`**: এই দুটি কম্পונেন্ট মিলে ড্যাশবোর্ডের নেভিগেশন তৈরি করে।
- **`add-product-dialog.tsx`, `edit-product-dialog.tsx`**: এই ফাইলগুলো মডাল বা ডায়ালগ বক্স তৈরি করে, যা নতুন পণ্য যোগ করা বা এডিট করার জন্য ব্যবহৃত হয়।

---

### 📁 `src/hooks/`
এই ফোল্ডারে কাস্টম হুক (Custom Hooks) থাকে, যা অ্যাপ্লিকেশনের বিভিন্ন অংশের ডেটা এবং লজিক পরিচালনা করে।

- **`use-app-data.tsx`**: এটি অ্যাপ্লিকেশনের প্রধান ডেটা ম্যানেজমেন্ট হুক। সমস্ত ডেটা (পণ্য, চালান, কর্মচারী) এবং তাদের পরিবর্তনের ফাংশনগুলো এখানে কেন্দ্রীভূত। এটি এখন ডাটাবেসের (`PostgreSQL`) সাথে সরাসরি কাজ করে, তবে ডাটাবেস সংযোগ না থাকলে `localStorage`-এ ফলব্যাক করে।
- **`use-user.tsx`**: ব্যবহারকারীর লগইন অবস্থা, ভূমিকা (role) এবং লগআউট ফাংশন পরিচালনা করে। এটি Firebase-এর সাথে সরাসরি যোগাযোগ করে।
- **`use-settings.tsx` ও `use-translation.tsx`**: অ্যাপের সেটিংস (যেমন প্রিন্ট ফরম্যাট, ভাষা) এবং বহুভাষিকতা (internationalization) পরিচালনা করে।

---

### 📁 `src/lib/`
এই ফোল্ডারে সহায়ক ফাংশন এবং কনফিগারেশন ফাইল থাকে।

- **`types.ts`**: পুরো অ্যাপ্লিকেশনে ব্যবহৃত ডেটার টাইপ (যেমন `Product`, `Invoice`) এখানে ডিফাইন করা হয়েছে।
- **`firebase/firebase.ts`**: Firebase প্রজেক্টের কনফিগারেশন কোড।
- **`actions/`**: Next.js সার্ভার অ্যাকশন। এই কোড শুধুমাত্র সার্ভারে চলে এবং সরাসরি ডেটাবেসের সাথে যোগাযোগ করে, যা অ্যাপ্লিকেশনকে নিরাপদ করে।

---

### 🐳 Docker এবং ডেটাবেস

আপনার অ্যাপ্লিকেশনটি এখন Docker ব্যবহার করে একটি সম্পূর্ণ পোর্টেবল সিস্টেম হিসেবে তৈরি।

- **`Dockerfile`**: আপনার Next.js অ্যাপ্লিকেশনকে একটি Docker ইমেজে পরিণত করার নির্দেশাবলী।
- **`docker-compose.yml`**: এই ফাইলটি একটিমাত্র কমান্ডে (`docker-compose up`) আপনার অ্যাপ্লিকেশন এবং একটি PostgreSQL ডেটাবেস উভয়কেই চালু করে। এটি ডেটাবেস তৈরি, নেটওয়ার্কিং এবং ডেটা সংরক্ষণের (volume) মতো জটিল কাজগুলো স্বয়ংক্রিয়ভাবে পরিচালনা করে।
- **`scripts/setup-db.ts`**: একটি সহায়ক স্ক্রিপ্ট (`npm run db:setup`) যা প্রথমবার চালানোর সময় আপনার Docker ডেটাবেসের ভেতরে প্রয়োজনীয় টেবিল তৈরি করে দেয়।

এই আর্কিটেকচারের ফলে, যেকোনো ডেভেলপার কোনো রকম পূর্ব প্রস্তুতি ছাড়াই মাত্র দুটি কমান্ডে আপনার সম্পূর্ণ অ্যাপ্লিকেশনটি (ডেটাবেসসহ) তার নিজের কম্পিউটারে চালাতে পারবে। এটি আন্তর্জাতিক মানের সফটওয়্যার বিতরণের একটি সেরা অনুশীলন।
